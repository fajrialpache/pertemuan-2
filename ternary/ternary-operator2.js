const age = 21;
/**
 * ternary operator dapat digunakan untuk menyimpan nilai kevariable
 */

const status = age > 21 ? "Dewasa" : "Belum Dewasa";
console.log(status);

// pr lu coba bikin kondisi baru minimal 5 kondisi
